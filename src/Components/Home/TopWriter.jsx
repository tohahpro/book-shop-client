import SectionTitle from "../Title/SectionTitle";


const TopWriter = () => {
    return (
        <div>
            <SectionTitle heading={'Top Writer In This Month'} />
        </div>
    );
};

export default TopWriter;